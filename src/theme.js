import { DefaultTheme } from 'styled-components';

const flex = {
  row: `
        display: flex;
        flex-direction: row;
    `,
  col: `
        display: flex;
        flex-direction: column;
    `,
};

const colors = {};

export const theme = { flex, colors };
